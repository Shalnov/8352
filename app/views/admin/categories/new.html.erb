<h1>Добавить категорию</h1>

<% form_for([:admin, @category]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => { :f => f } %>

  <p>
    <%= f.submit "Создать" %>
  </p>
<% end %>
