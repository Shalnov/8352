<h2>Экспорт данныъ в каталог</h2>
<p>Экспортировано: <%= @exported_records %></p>
<p>Новых записей: <%= @updated_records %></p>


<% if @new_categories %>
<h2>Новые категории</h2>
<% form_tag :action => 'set_categories' do |f|%>
<%= hidden_field "source", "id" %>
  <table>
    <tr>
      <th>Источник</th>
      <th>Каталог</th>
    </tr>
  <% @new_categories.each_with_index do |rc, i| %>
    <tr>
      <td><%= hidden_field "categories[#{i}]", "title", :value => rc %><%= rc %></td>
      <td><%= select "categories[#{i}]", "id", @category_for_select %></td>
    </tr>
  <% end %>
  </table>

  <%= submit_tag "Установить" %>
<% end %>

<% else %>
<h2>Новых категорий не обнаружено</h2>
<% end %>



<%# quick_edit :message => 'Edit this page', 
               :path => "admin/sources/edit/21" %>
