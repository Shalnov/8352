<h1>Редактирование компании</h1>

<% form_for([:admin, @company]) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => "form", :locals => { :f => f }%>
  <p>
    <%= f.submit "Изменить" %>
  </p>
<% end %>
