%h1 Категории

%table.list#branches_list
  %thead
    %th{:width => 16}
    %th{:width => 16}
    %th{:width => 16}    
    %th{:width => 16} ID
    %th{:width => "%"} Наименование
    %th{:width => 16}
    %th{:width => 16}
  %tbody#branches
    = render "branches"

#groups_container
  %table.list
    %thead
      %th{:width => 16}
      %th{:width => 16} ID
      %th{:width => "%"} Наименование
      %th{:width => "%"} ID Веток
      %th{:width => "30"}      
    %tbody#groups
      = render "groups"

:javascript
  $(document).ready(function() {
    branches();
  });
