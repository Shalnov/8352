indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /home/ralph/projects/8352/log/searchd.log
  query_log = /home/ralph/projects/8352/log/searchd.query.log
  pid_file = /home/ralph/projects/8352/log/searchd.development.pid
}

source company_core_0
{
  type = pgsql
  sql_host = 127.0.0.1
  sql_user = root
  sql_pass = 
  sql_db = d8352_dev
  sql_query = SELECT "companies"."id" * 1 + 0 AS "id" , "companies"."name" AS "name", "companies"."description" AS "description", "companies"."address" AS "address", "companies"."name" AS "category", array_to_string(array_accum(COALESCE(CAST("phones"."number" as varchar), '') || ' ' || COALESCE(CAST("phones"."person" as varchar), '')), ' ') AS "phone", "companies"."email" AS "emails", COALESCE("companies"."name", '') AS "name_sort", "companies"."id" AS "sphinx_internal_id", 2147627219 AS "class_crc", '2147627219' AS "subclass_crcs", 0 AS "sphinx_deleted" FROM companies    LEFT OUTER JOIN "phones" ON phones.company_id = companies.id  WHERE "companies"."id" >= $start   AND "companies"."id" <= $end    GROUP BY "companies"."id", "companies"."name", "companies"."description", "companies"."address", "companies"."name", "companies"."email", "companies"."name", "companies"."id" 
  sql_query_range = SELECT COALESCE(MIN("id"), 1), COALESCE(MAX("id"), 1) FROM "companies" 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_str2ordinal = name_sort
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM "companies" WHERE  "id" = (($id - 0) / 1)
}

index company_core
{
  source = company_core_0
  path = /home/ralph/projects/8352/db/sphinx/development/company_core
  morphology = stem_en
  charset_type = utf-8
  min_infix_len = 1
  enable_star = 1
}

index company
{
  type = distributed
  local = company_core
}
