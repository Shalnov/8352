git clone
git submodule init
git submodule update
rake gems:install
rake db:create
rake db:migrate
rake db:fill_roles

createlang plpgsql d8352 
rake db:p8352:add_default_roles  # add default roles: "admin", "editor", "reviewer"
rake db:p8352:add_default_users  # add default user "admin" with attributes:
				 #   email="username@some-domain-name.com",
				 #   password="admin123"
				 #   role="admin"
rake db:p8352:add_sources	 # add sources from libs/grabber/*

# зарегистрировать пользователя admin

rake db:make_admin_by_login login=admin

./script/server

# sphinx
1. Прописываешь индексы
2. Комментируешь acts_as_taggable в company.rb
3. rake thinking_sphinx:index
4. Разкомментируешь acts_as_taggable
5. rake thinking_sphinx:start

haml --rails .



# TODO
# Использовать http://jquery.sanchezsalvador.com/page/jquerycombobox.aspx как tree combobox
